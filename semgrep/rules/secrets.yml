rules:
  - id: hardcoded-secret
    pattern: |
      const $S = "SECRET_" ...
    message: "Segredo possivelmente hardcoded. Use variáveis de ambiente/secret manager."
    severity: HIGH
    languages: [javascript, typescript, python, java]
    metadata:
      fix: "Mover para Secret Manager / .env e referenciar com process.env/ENV."
