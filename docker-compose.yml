# docker-compose.yml
services:
  app:
    image: node:20-alpine
    working_dir: /app
    command: sh -c "npm ci || npm i && node src/index.js"
    volumes:
      - ./:/app
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"   # <— IMPORTANTE: publicar a porta
